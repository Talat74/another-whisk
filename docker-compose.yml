version: "3.8"

services:
  app:
    build: .
    container_name: anotherwhisk_ap 
  
  nginx: 
    image: nginx:latest
    container_name: anotherwhisk_nginx
    ports:
      - "80:80"
    volumes:
       - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app 
